apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    name: app-service
    app: clojure-spa-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 3005
  selector:
    name: app-pod
    app: clojure-spa-app