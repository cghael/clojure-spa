apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    name: app-service
    app: clojure-spa-app
spec:
  type: NodePort
  ports:
    - port: 4000
      targetPort: 80
      nodePort: 30005
  selector:
    name: app-pod
    app: clojure-spa-app